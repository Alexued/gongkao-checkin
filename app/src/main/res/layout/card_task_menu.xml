<?xml version="1.0" encoding="utf-8"?>
<!-- 任务卡长按菜单：全屏遮罩 + 一张跟着手指定位的小卡 -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <View
        android:id="@+id/menuScrim"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- 宽度在代码里改成被长按那一行的宽度，让它盖住那张卡 -->
    <LinearLayout
        android:id="@+id/menuCard"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_glass_menu"
        android:clipToOutline="true"
        android:elevation="24dp"
        android:orientation="vertical"
        android:paddingTop="6dp"
        android:paddingBottom="6dp">

        <!-- 标题沿用任务行的样子（左侧色条 + 标题），过渡才不突兀 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingHorizontal="16dp"
            android:paddingTop="8dp"
            android:paddingBottom="10dp">

            <View
                android:id="@+id/menuColorBar"
                android:layout_width="3dp"
                android:layout_height="16dp"
                android:background="@color/accent" />

            <TextView
                android:id="@+id/menuTitle"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="10dp"
                android:layout_weight="1"
                android:ellipsize="end"
                android:fontFamily="sans-serif-medium"
                android:maxLines="1"
                android:textColor="@color/ink"
                android:textSize="15sp" />
        </LinearLayout>

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginHorizontal="16dp"
            android:layout_marginBottom="4dp"
            android:background="@color/divider_glass" />

        <TextView
            android:id="@+id/menuEdit"
            style="@style/TaskMenuItem"
            android:drawableStart="@drawable/ic_edit"
            android:text="@string/menu_edit" />

        <TextView
            android:id="@+id/menuFocus"
            style="@style/TaskMenuItem"
            android:drawableStart="@drawable/ic_timer"
            android:text="@string/menu_focus" />

        <TextView
            android:id="@+id/menuReorder"
            style="@style/TaskMenuItem"
            android:drawableStart="@drawable/ic_reorder"
            android:text="@string/menu_reorder" />

        <!-- 删除单独一组：上面留一道线隔开，图标和文字都用警示色 -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginHorizontal="16dp"
            android:layout_marginTop="4dp"
            android:layout_marginBottom="4dp"
            android:background="@color/divider_glass" />

        <TextView
            android:id="@+id/menuDelete"
            style="@style/TaskMenuItem"
            android:drawableStart="@drawable/ic_trash"
            android:drawableTint="@color/rose"
            android:text="@string/menu_delete"
            android:textColor="@color/rose" />
    </LinearLayout>
</FrameLayout>
