<?xml version="1.0" encoding="utf-8"?>
<!--
  卡片质感相关的主题属性。shape drawable 里可以直接引用 ?attr/，
  所以换主题只需要覆盖这几项，不用逐个改布局里的 @color 引用。
-->
<resources>
    <!--
      页面整体底色。页面根布局一律引用 ?attr/pageFill 而不是 @color/bg，
      这样"页面底色"和"调色板里的 bg"能分开：玻璃主题要纯白页面，但调色板仍是亮色那套。
    -->
    <attr name="pageFill" format="color" />

    <!-- 卡片/面板底色 -->
    <attr name="cardFill" format="color" />
    <!-- 卡片描边；不需要描边的主题设成 transparent -->
    <attr name="cardStroke" format="color" />
    <attr name="cardStrokeWidth" format="dimension" />
    <!-- 次级面板（pill / 输入框）底色 -->
    <attr name="softFill" format="color" />

    <!--
      浮层（长按菜单这类）的玻璃底。跟 cardFill 分开是因为浮层背后被真实模糊过，
      要比普通卡片更透才看得出模糊，描边也要更亮才压得住背后花掉的画面。
    -->
    <attr name="glassFill" format="color" />
    <attr name="glassStroke" format="color" />
</resources>
